/**
 * Re-export: keep `import … from "./seo/SeoHead"` working across the app.
 * Canonical, Open Graph, and Twitter tags live in `../components/SEO.jsx`.
 */
export { default } from "../components/SEO.jsx";
